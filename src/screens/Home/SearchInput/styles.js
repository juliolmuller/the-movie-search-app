import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#ccc',
    borderRadius: 12,
    padding: 8,
    margin: 10,
  },
  input: {
    flexGrow: 1,
    fontSize: 20,
    marginLeft: 8,
  },
})

export default styles
